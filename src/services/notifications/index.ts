/**
 * Themed barrel — Notifications
 * Non-breaking: re-exports existing services to enable clearer imports.
 * Usage (recommended):
 *   import { NotificationSchedulerService } from '@/services/notifications';
 */
export * from '../NotificationSchedulerService';
